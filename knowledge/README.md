# 知识库目录

将你的 Markdown（`.md`）文件放在此目录或其子目录中。

## 使用步骤

1. 把 `.md` 文件复制到 `knowledge/` 目录
2. 启动后端与前端
3. 在 Web UI 左侧点击 **重建索引**
4. 在聊天框中提问

## 建议

- 每个文件聚焦一个主题，便于引用定位
- 使用 `# 标题` 作为文档标题（会显示在引用来源中）
- 支持子目录，例如 `knowledge/product/faq.md`
